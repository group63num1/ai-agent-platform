<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.app.mapper.AgentSessionMapper">

    <resultMap id="AgentSessionResultMap" type="com.example.demo.app.entity.AgentSession">
        <id column="session_id" property="sessionId"/>
        <result column="agent_id" property="agentId"/>
        <result column="name" property="name"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.demo.app.entity.AgentSession">
        INSERT INTO agent_sessions (session_id, agent_id, name, created_at, updated_at)
        VALUES (#{sessionId}, #{agentId}, #{name}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectById" resultMap="AgentSessionResultMap">
        SELECT session_id, agent_id, name, created_at, updated_at
        FROM agent_sessions
        WHERE session_id = #{sessionId}
    </select>

    <select id="selectByAgentId" resultMap="AgentSessionResultMap">
        SELECT session_id, agent_id, name, created_at, updated_at
        FROM agent_sessions
        WHERE agent_id = #{agentId}
        ORDER BY updated_at DESC
    </select>

    <delete id="deleteById">
        DELETE FROM agent_sessions WHERE session_id = #{sessionId}
    </delete>
</mapper>

