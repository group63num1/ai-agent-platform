<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.app.mapper.ChatSessionMapper">

    <resultMap id="ChatSessionResultMap" type="com.example.demo.app.entity.ChatSession">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="title" property="title" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="is_deleted" property="isDeleted" />
    </resultMap>

    <sql id="ChatSession_Column_List">
        id, user_id, title, created_at, updated_at, is_deleted
    </sql>

    <insert id="insert" parameterType="com.example.demo.app.entity.ChatSession"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_session (
            user_id, title, created_at, updated_at, is_deleted
        ) VALUES (
            #{userId}, #{title}, NOW(), NOW(), 0
        )
    </insert>

    <select id="selectById" resultMap="ChatSessionResultMap">
        SELECT <include refid="ChatSession_Column_List" />
        FROM chat_session
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByUserId" resultMap="ChatSessionResultMap">
        SELECT <include refid="ChatSession_Column_List" />
        FROM chat_session
        WHERE user_id = #{userId}
        <if test="includeDeleted == null or includeDeleted == false">
            AND is_deleted = 0
        </if>
        ORDER BY updated_at DESC
    </select>

    <update id="update" parameterType="com.example.demo.app.entity.ChatSession">
        UPDATE chat_session
        <set>
            <if test="title != null">title = #{title},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="softDelete">
        UPDATE chat_session
        SET is_deleted = 1, updated_at = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="updateTitle">
        UPDATE chat_session
        SET title = #{title}, updated_at = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>

</mapper>

